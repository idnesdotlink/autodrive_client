import {Component} from '@angular/core'
import {MaskDirective, MaskPipe} from '@extra/input-mask'

@Component({
  selector: 'split-input',
  templateUrl: 'template.html'
})
export class SplitInputComponent {
  test: "0";
}
